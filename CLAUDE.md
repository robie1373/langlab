# langlab

Context for this project lives in The Ledger: `~/ledger2/langlab.md`

Read that page before doing any work here. It contains the stack, deployment,
data paths, and the full API reference.

**Feature work:** execute `docs/specs/` in numbered order. Read
`docs/specs/README.md` first — it holds the hard constraints (stdlib only, no
build step, no romanization, localtime discipline) and code conventions. The
specs are decision-complete; do not re-open decided questions.

Work is coordinated through The Bearing (`~/work/`).
Archive of the original CLAUDE.md: `~/archive/2026-04-27/langlab-CLAUDE.md`
